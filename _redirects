# Netlify / Cloudflare Pages
# Legacy query URLs → canonical pretty paths (force, even if file exists)
/project.html slug=:slug  /project/:slug/  301!
/service.html slug=:slug  /service/:slug/  301!

# Missing URL → branded 404
/*    /404.html   404
